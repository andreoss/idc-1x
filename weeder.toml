# Idc.Import.renderRow is a CSV round-trip test helper (paired with
# parseLine in property tests) with no production call site by design.
roots = ["Main.main","^Paths_.*","Idc.Import.renderRow"]

type-class-roots = true

root-instances = [{ class = "\\.IsString$" },{ class = "\\.IsList$" }]

unused-types = false

root-modules = []
